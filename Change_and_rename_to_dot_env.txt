POSTGRES_DB_MAIN=kt-db
POSTGRES_DB_SHADOW=shadow-db

POSTGRES_USER=postgres
POSTGRES_PASSWORD=ADD PASSWORD HERE

PRISMA_USER=prisma_user
PRISMA_PASSWORD=ADD PASSWORD HERE

ADMIN_USER=admin
ADMIN_PASSWORD=ADD PASSWORD HERE

DATABASE_URL=postgresql://${PRISMA_USER}:${PRISMA_PASSWORD}@postgres:5432/kt-db

SHADOW_DATABASE_URL=postgresql://${PRISMA_USER}:${PRISMA_PASSWORD}@postgres:5432/shadow-db

ACCESS_SECRET=random 64 hex (openssl rand -hex 64)
REFRESH_SECRET=random 64 hex (openssl rand -hex 64)